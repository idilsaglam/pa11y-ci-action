{
  "urls": [
    "https://example.com/"
  ],
  "defaults": {
    "standard": "WCAG2AA",
    "wait": 1000,
    "timeout": 30000,
    "chromeLaunchConfig": {
      "args": [
        "--no-sandbox",
        "--disable-setuid-sandbox",
        "--disable-dev-shm-usage"
      ]
    }
  },
  "json": true,
  "reporter": "json",
  "output": "pa11y-report.json"
}
